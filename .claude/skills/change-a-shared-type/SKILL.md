---
name: change-a-shared-type
description: Alter a shared type without making every tool upgrade at once. Use before touching anything in lumastack/luma-catalog/luma-types, or any type a second consumer already reads.
---

<!-- luma-foreman:generated from lumastack/luma-catalog/luma-maintainers workflows/change-a-shared-type. Regenerate with `luma-foreman apply`; edits are lost. -->

# Change a type more than one tool depends on

**Read `.luma/bundles/lumastack/luma-catalog/luma-maintainers/workflows/change-a-shared-type.md` and follow it.** That file is the workflow. This is the adapter that makes it reachable from here, and it deliberately carries no copy of it — the copy would drift.

From the `lumastack/luma-catalog/luma-maintainers` bundle, vendored at `.luma/bundles/lumastack/luma-catalog/luma-maintainers/`. Do not edit anything under there — an adopted bundle is a copy, and editing it is drift.
