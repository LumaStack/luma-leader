---
name: verify-audit
description: Check whether each finding is genuinely resolved and record a disposition. Use after a response is filed — this is what closes the loop.
---

<!-- luma-foreman:generated from luma/audit-records workflows/verify-audit. Regenerate with `luma-foreman outfit`; edits are lost. -->

# Verify an audit response

**Read `.luma/bundles/luma/audit-records/workflows/verify-audit.md` and follow it.** That file is the workflow. This is the adapter that makes it reachable from here, and it deliberately carries no copy of it — the copy would drift.

From the `luma/audit-records` bundle, vendored at `.luma/bundles/luma/audit-records/`. Do not edit anything under there — an adopted bundle is a copy, and editing it is drift.
