---
name: verify-audit
description: Check whether each finding is genuinely resolved and record a disposition. Use after a response is filed — this is what closes the loop.
---

<!-- luma-foreman:generated from lumastack/luma-catalog/audit-records procedure/verify-audit. Regenerate with `luma-foreman apply`; edits are lost. -->

# Verify an audit response

**Read `.luma/bundles/lumastack/luma-catalog/audit-records/procedure/verify-audit.md` and follow it.** That file is the procedure. This is the adapter that makes it reachable from here, and it deliberately carries no copy of it — the copy would drift.

Required reading from this bundle — open before following the procedure:

- `.luma/bundles/lumastack/luma-catalog/audit-records/policy/audit-layout.md` — Where an audit lives, how it is named, and the three-party loop — auditor, respondent, auditor again — that makes the record settle rather than accumulate.

From the `lumastack/luma-catalog/audit-records` bundle, vendored at `.luma/bundles/lumastack/luma-catalog/audit-records/`. Do not edit anything under there — an adopted bundle is a copy, and editing it is drift.
