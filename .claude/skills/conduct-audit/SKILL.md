---
name: conduct-audit
description: Examine something, record what is wrong and why, and leave a record somebody else can act on. Use when auditing a codebase, a process, or any artifact that has a commit.
---

<!-- luma-foreman:generated from lumastack/luma-catalog/audit-records workflows/conduct-audit. Regenerate with `luma-foreman apply`; edits are lost. -->

# Conduct an audit

**Read `.luma/bundles/lumastack/luma-catalog/audit-records/workflows/conduct-audit.md` and follow it.** That file is the workflow. This is the adapter that makes it reachable from here, and it deliberately carries no copy of it — the copy would drift.

From the `lumastack/luma-catalog/audit-records` bundle, vendored at `.luma/bundles/lumastack/luma-catalog/audit-records/`. Do not edit anything under there — an adopted bundle is a copy, and editing it is drift.
