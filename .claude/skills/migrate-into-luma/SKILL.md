---
name: migrate-into-luma
description: Move an existing project's scattered conventions, decisions and notes into .luma/. Use on a project that already has this material somewhere else.
---

<!-- luma-foreman:generated from lumastack/luma-catalog/luma-tools workflows/migrate-into-luma. Regenerate with `luma-foreman apply`; edits are lost. -->

# Migrate into luma

**Read `.luma/bundles/lumastack/luma-catalog/luma-tools/workflows/migrate-into-luma.md` and follow it.** That file is the workflow. This is the adapter that makes it reachable from here, and it deliberately carries no copy of it — the copy would drift.

From the `lumastack/luma-catalog/luma-tools` bundle, vendored at `.luma/bundles/lumastack/luma-catalog/luma-tools/`. Do not edit anything under there — an adopted bundle is a copy, and editing it is drift.
