---
name: prune-archived-decisions
description: Permanently remove decision records that have been archived longer than the retention period. Only reaches `archived/`, never a live decision. Rarely the right call.
---

<!-- luma-foreman:generated from lumastack/luma-catalog/decision-records procedure/prune-archived-decisions. Regenerate with `luma-foreman apply`; edits are lost. -->

# Prune archived decisions

**Read `.luma/bundles/lumastack/luma-catalog/decision-records/procedure/prune-archived-decisions.md` and follow it.** That file is the procedure. This is the adapter that makes it reachable from here, and it deliberately carries no copy of it — the copy would drift.

From the `lumastack/luma-catalog/decision-records` bundle, vendored at `.luma/bundles/lumastack/luma-catalog/decision-records/`. Do not edit anything under there — an adopted bundle is a copy, and editing it is drift.
