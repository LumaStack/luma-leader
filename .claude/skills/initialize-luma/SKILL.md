---
name: initialize-luma
description: Stand up .luma/ in a repository that does not have one. Install foreman if it is missing, then run init. Use when setting up luma in a project for the first time.
---

<!-- luma-foreman:generated from lumastack/luma-catalog/luma-tools workflows/initialize-luma. Regenerate with `luma-foreman apply`; edits are lost. -->

# Initialize luma

**Read `.luma/bundles/lumastack/luma-catalog/luma-tools/workflows/initialize-luma.md` and follow it.** That file is the workflow. This is the adapter that makes it reachable from here, and it deliberately carries no copy of it — the copy would drift.

From the `lumastack/luma-catalog/luma-tools` bundle, vendored at `.luma/bundles/lumastack/luma-catalog/luma-tools/`. Do not edit anything under there — an adopted bundle is a copy, and editing it is drift.
