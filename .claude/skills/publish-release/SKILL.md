---
name: publish-release
description: Verify the gh CLI is installed and authenticated, then cut and publish a release. Use when asked to cut, tag, or publish a release.
---

<!-- luma-foreman:generated from lumastack/luma-catalog/github-release procedure/publish-release. Regenerate with `luma-foreman apply`; edits are lost. -->

# Publish a GitHub release

**Read `.luma/bundles/lumastack/luma-catalog/github-release/procedure/publish-release.md` and follow it.** That file is the procedure. This is the adapter that makes it reachable from here, and it deliberately carries no copy of it — the copy would drift.

From the `lumastack/luma-catalog/github-release` bundle, vendored at `.luma/bundles/lumastack/luma-catalog/github-release/`. Do not edit anything under there — an adopted bundle is a copy, and editing it is drift.
