---
name: describe-project
description: Write or refresh the descriptor a repository publishes about itself at .luma/PROJECT.md. Use when a repository is one of several, or when what it is for has changed.
---

<!-- luma-foreman:generated from lumastack/luma-catalog/project-documentation procedure/describe-project. Regenerate with `luma-foreman apply`; edits are lost. -->

# Describe a project

**Read `.luma/bundles/lumastack/luma-catalog/project-documentation/procedure/describe-project.md` and follow it.** That file is the procedure. This is the adapter that makes it reachable from here, and it deliberately carries no copy of it — the copy would drift.

Required reading from this bundle — open before following the procedure:

- `.luma/bundles/lumastack/luma-catalog/project-documentation/policy/readme.md` — A README answers what this is, why it exists, and where to go next — in that order. What belongs in one, what does not, and why the limit matters.

From the `lumastack/luma-catalog/project-documentation` bundle, vendored at `.luma/bundles/lumastack/luma-catalog/project-documentation/`. Do not edit anything under there — an adopted bundle is a copy, and editing it is drift.
