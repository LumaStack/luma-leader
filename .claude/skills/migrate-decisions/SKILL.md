---
name: migrate-decisions
description: Split a single DECISIONS.md into individual records, reconstruct what supersedes what, repoint everything that linked to the file, and only then remove it. Use once per project that has one.
---

<!-- luma-foreman:generated from lumastack/luma-catalog/decision-records procedure/migrate-decisions. Regenerate with `luma-foreman apply`; edits are lost. -->

# Migrate a DECISIONS file

**Read `.luma/bundles/lumastack/luma-catalog/decision-records/procedure/migrate-decisions.md` and follow it.** That file is the procedure. This is the adapter that makes it reachable from here, and it deliberately carries no copy of it — the copy would drift.

From the `lumastack/luma-catalog/decision-records` bundle, vendored at `.luma/bundles/lumastack/luma-catalog/decision-records/`. Do not edit anything under there — an adopted bundle is a copy, and editing it is drift.
