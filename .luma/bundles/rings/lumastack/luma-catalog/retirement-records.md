<!-- Generated by `luma-foreman apply`. Edits are lost. -->

# `lumastack/luma-catalog/retirement-records` 0.2.1

Retiring an idea across many projects — the decision that stays home, the strategy that travels, and the recognizers that find a concept whose vocabulary survived.

**Start at `policy/retiring-a-concept`.**

**Read these now.** Each declares `matches: always`, so nothing gates when it governs — which means everything done with this bundle.

- `.luma/bundles/lumastack/luma-catalog/retirement-records/policy/what-we-retired.md` — Every idea this estate has retired, one line each — what it was, what replaced it. The list an author needs before writing, not after.

**3 workflow(s) here reach you as skills, not through this ring** — invoke by name: `release-a-retirement`, `retire-a-concept`, `sweep-retirements`.

In `.luma/bundles/lumastack/luma-catalog/retirement-records/`:

- `policy/retiring-a-concept` (policy) — What may be retired, how far it reaches, and why a word is the cheapest recognizer rather than the important one. Read before retiring anything.
  - matches: topic:retiring an idea, a concept, a field or a word, topic:deciding how widely a change should be enforced
