# Changelog — `luma/project`

The history of the `luma/project` Type Definition, newest first, keyed by the
type's own `version`. The changelog opens at the version in force when it
was started; earlier bumps are in this repository's git history — no
retroactive backfill.

## 0.2.0

- The changelog begins at this version, which was already in force: the
  catalog's migration to LKF v0.0.21 (2026-09-19) moved every Type Definition
  to the folder shape, so this definition now lives at
  `type_definitions/project/DEFINITION.md`, with this changelog beside it.
  The contract is unchanged from the `0.2.0` that shipped before the
  move.
