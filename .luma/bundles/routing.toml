# Generated by `luma-foreman outfit`. Edits are lost.
#
# Every Document that says when it applies, and what happens when it is
# not complied with. The permission gate reads the rows that block; it
# cannot be told to ignore them, and nothing in `agent-permissions`
# writes here.

[[rule]]
bundle = "luma/luma-maintainers"
document = "policy/the-estate"
title = "The estate, and what each repository owns"
path = ".luma/bundles/luma/luma-maintainers/policy/the-estate.md"
on_violation = "allow"
applies_to = ["topic:deciding which repository a new thing belongs in"]

[[rule]]
bundle = "luma/luma-tools"
document = "policy/what-each-tool-does"
title = "What each luma tool does"
path = ".luma/bundles/luma/luma-tools/policy/what-each-tool-does.md"
on_violation = "allow"
applies_to = ["command:luma-foreman", "command:luma-catalog-curator", "topic:choosing which luma tool does a job"]
